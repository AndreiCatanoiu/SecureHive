<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SecureHive" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_variant</item>
        <item name="colorPrimaryVariant">@color/primary</item>
        <item name="colorOnPrimary">@color/text_primary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary_variant</item>
        <item name="colorSecondaryVariant">@color/secondary</item>
        <item name="colorOnSecondary">@color/text_primary</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:navigationBarColor">@color/primary</item>
        <!-- Culori pentru text -->
        <item name="android:textColorPrimary">#FFFFFF</item>
        <item name="android:textColorSecondary">#B3FFFFFF</item>
        <!-- Culori pentru fundal -->
        <item name="android:windowBackground">#121212</item>
        <item name="colorSurface">#1E1E1E</item>
        <item name="colorOnSurface">#FFFFFF</item>
        <!-- Stiluri pentru carduri -->
        <item name="materialCardViewStyle">@style/Widget.SecureHive.CardView.Dark</item>
        <!-- Stiluri pentru butoane -->
        <item name="materialButtonStyle">@style/Widget.SecureHive.Button.Dark</item>
    </style>

    <!-- Stil personalizat pentru butoane în modul întunecat -->
    <style name="Widget.SecureHive.Button.Dark" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/primary_variant</item>
        <item name="android:textColor">#000000</item>
        <item name="cornerRadius">8dp</item>
        <item name="android:paddingLeft">24dp</item>
        <item name="android:paddingRight">24dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Stil pentru butoane secundare în modul întunecat -->
    <style name="Widget.SecureHive.Button.Secondary.Dark" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="strokeColor">@color/secondary_variant</item>
        <item name="android:textColor">@color/secondary_variant</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <!-- Stil pentru carduri în modul întunecat -->
    <style name="Widget.SecureHive.CardView.Dark" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">#1E1E1E</item>
        <item name="cardElevation">2dp</item>
        <item name="cardCornerRadius">8dp</item>
        <item name="contentPadding">16dp</item>
        <item name="strokeWidth">1dp</item>
        <item name="strokeColor">#2C2C2C</item>
    </style>
</resources>